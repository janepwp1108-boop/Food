import COLORS from "./colors";

const THEME = {
  colors: COLORS,

  radius: {
    sm: 10,
    md: 18,
    lg: 25,
  },

  spacing: {
    xs: 5,
    sm: 10,
    md: 15,
    lg: 20,
    xl: 30,
  },
};

export default THEME;